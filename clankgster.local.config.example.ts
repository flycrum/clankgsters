/** Developer-only tier-2 overrides loaded after `clankgster.config.ts`; keep personal preferences here and leave this file uncommitted. */
const clankgsterLocal = {
  loggingEnabled: true,
  agents: {},
};

export default clankgsterLocal;
