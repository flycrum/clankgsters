---
name: skills-update-context
description: >-
  Updates standalone source pathway `skills/` files while preserving structure,
  quality checks, and trigger-oriented descriptions. Use when refining an
  existing standalone SKILL.md workflow.
---

# Update standalone skills context

## Steps

1. Read existing `SKILL.md`.
2. Apply requested changes.
3. Re-check frontmatter and trigger phrases.
4. Produce finalized update output.

This skill is also the target of MCP route `skills.update` (`SkillsUpdate`).

## Verification

- [ ] Existing workflow intent preserved
- [ ] YAML frontmatter remains valid
- [ ] Description still discoverable
