# Clankgsters sync output (regenerated locally; not source of truth)
.clankgsters-cache/
.superagent/

# Library-specific files
clankgsters.local.config.ts

# Ignore root-level Claude and Cursor plugin dirs (sync scripts; do not commit)
# Note: plugin dirs under .clank/ (e.g. .clank/plugins/*/.claude-plugin/) remain tracked (as required by plugin marketplace sync)
/.claude-plugin/
/.cursor-plugin/
# Recursively ignore these (not required by the system definitions)
.claude/
.cursor/

# Clankgsters: potentially generated agent context files (any depth)
**/AGENTS.md
# Clankgsters: Codex marketplace section (potentially generated by clankgsters sync)
AGENTS.override.md
# Clankgsters: symlinked from CLANK.md for Claude (when Claude marketplace sync is enabled)
CLAUDE.md

# Dependencies
node_modules/

# Build outputs
dist/

# IDE / OS
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.example.json
.DS_Store
*.log

